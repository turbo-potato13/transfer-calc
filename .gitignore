# Реальные идентификаторы сервера: OCID, IP, VCN, доступ. Публиковать нечего и незачем.
SERVER.md

# Секреты на сервере (токен DuckDNS и прочее)
.env

# Генерируется cron на сервере — в репозитории ему делать нечего,
# иначе он подрался бы с git pull --ff-only.
site/rates.json

# Служебное
*.bak
~$*.xlsx
.DS_Store
